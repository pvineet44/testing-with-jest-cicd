version: 2
jobs:
  build:
      docker:
      # specify the version you desire here
      - image: circleci/node:15.11.0
    
    working_directory: ~/repo

    steps:
      - run: npm install
      - run: npm test